const array = [2, 1, 20, 40, 3, 60, 30]
// Esto lo que hace es convertir los números del array a cadenas de texto
console.log(array.sort())

const numero = 1
const cadena = '1'

// Compara valores
console.log(numero == cadena)
console.log(numero === cadena)